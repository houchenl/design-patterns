
定义： 提供一种方法访问容器对象中的各个元素，而又不暴露对象的内部细节。

角色：
迭代器角色(Iterator)： 负责定义访问和遍历元素的接口
具体迭代器角色(ConcreteIterator)： 实现迭代器接口，并要记录遍历中的当前位置
容器角色(Container)： 负责提供创建具体迭代器角色的接口
具体容器角色(ConcreteContainer)： 实现创建具体迭代器角色的接口，这个具体迭代器角色与该容器的结构相关